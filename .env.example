# Provider：openrouter（默认，提交验收）或 deepseek（显式测试）
POEM_PROVIDER=openrouter

# OpenRouter 免费路由（默认 provider）
OPENROUTER_API_KEY=

# DeepSeek flash（仅当 POEM_PROVIDER=deepseek 时使用）
DEEPSEEK_API_KEY=
DEEPSEEK_THINKING=disabled
DEEPSEEK_MAX_TOKENS=1024

# 运行开关
POEM_OFFLINE=0
POEM_DEBUG=0
